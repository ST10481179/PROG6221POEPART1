<Window x:Class="CyberSecurityChatbot.MainWindow"
        x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cybersecurity Awareness Bot"
        Height="620"
        Width="760"
        ResizeMode="NoResize"
        Background="WhiteSmoke"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10,10,10,14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" MinHeight="155" />
            <RowDefinition />
            <RowDefinition Height="Auto" MinHeight="65.04" />
        </Grid.RowDefinitions>

        <Border Background="#FF003C59" CornerRadius="10" Padding="14" Grid.Row="0">
            <StackPanel>
                <TextBlock Text="Cybersecurity Awareness Bot" Foreground="White" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" />
                <Border BorderBrush="#FF003C59" BorderThickness="1" Height="100">
                    <Image Width="365" Source="/Screenshot 2026-05-25 071115.png"/>
                </Border>
            </StackPanel>
        </Border>

        <TextBox x:Name="HistoryTextBox"
                 Grid.Row="1"
                 Margin="0,25,0,0"
                 Width="720"
                 HorizontalAlignment="Center"
                 IsReadOnly="True"
                 AcceptsReturn="True"
                 VerticalScrollBarVisibility="Auto"
                 TextWrapping="Wrap"
                 FontFamily="Consolas"
                 FontSize="12"
                 Background="White"
                 Foreground="LightGreen"
                 BorderBrush="Gray"
                 BorderThickness="1" Height="314" VerticalAlignment="Top" />

        <Grid Grid.Row="2" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBox x:Name="InputTextBox"
                     Grid.Column="0"
                     Width="560"
                     Height="42"
                     VerticalContentAlignment="Center"
                     FontSize="14"
                     Margin="0,0,10,0"
                     KeyDown="InputTextBox_KeyDown" />
            <Button x:Name="SendButton"
                    Grid.Column="1"
                    Width="140"
                    Height="42"
                    Content="Send"
                    Background="#FF75FF75"
                    Foreground="White"
                    FontWeight="Bold"
                    Click="SendButton_Click" />
        </Grid>
    </Grid>
</Window>